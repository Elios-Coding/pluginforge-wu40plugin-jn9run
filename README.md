# Wu40Plugin

Generated by **PluginForge AI**.

- Minecraft API: `1.21`
- Server type: `Paper`

## Build

The included GitHub Actions workflow builds this plugin automatically on every push to `main`.
The compiled `.jar` file is published as a GitHub Release.

## Manual build

```
mvn clean package
```

The jar will be in `target/`.
